---
description: 用户画像、沟通习惯、环境约束与技术偏好（跨项目通用）
tags: [user, profile, preferences, global]
created: 2026-09-13
---

# User Profile

## 基本信息
- 称呼：陈建鹏（GitHub 账号 `JianpChen`）
- 交流语言：**中文**，回复也用中文
- 系统：Windows（`Windows_NT 10.0.26200`），shell 用 Git Bash
- 目录习惯：学习和项目都放在 `D:/工作学习/` 下，路径**常含中文**

## 沟通风格偏好
- **结论先行**：先给结果/答案，再给依据
- 喜欢**表格汇总**（状态、对比、方案选择），一眼能扫完
- 要**可直接复制执行的完整命令**，不想要伪代码
- 破坏性操作（删除、覆盖）之前先**备份**，并说明**如何还原**
- 术语用中文，必要时附英文原词

## 环境约束（踩过的坑，很重要）
- **直连 `github.com` 不通**（Connection reset / 超时），必须走本地代理 `http://127.0.0.1:7897`
- 机器上**没有 Visual Studio / C++ 构建工具**：任何需要 `node-gyp` **本地编译**的 npm 原生模块都会失败，必须找预编译二进制或跳过
- 常用工具版本：git 2.55、Python 3.13、Node 22（pi 自带）、**没有 jq**

## 技术偏好
- 偏好**纯标准库、零第三方依赖**的方案（方便整体复制移植到无网机器）
- 偏好**离线优先**设计：不要联网下载模型、不要云端依赖
- 交付物倾向：**可整体复制的目录** + 打包成 zip

## 背景与在学内容
- 正在学习**软件测试入门**（测试思维、等价类划分、边界值分析、bug 报告要素）
- 研究 pi 的记忆系统：做过 `pi-memory-agent`（多记忆体 + 离线检索）、`memory-profiles`、`cpp-defect-review`（C/C++ 缺陷审查 skill）
