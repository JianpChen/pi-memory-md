---
description: 2026-09-13 会话操作记录：git 推送、卸载旧记忆插件、安装配置 pi-memory-md、迁移旧记忆、污染分析
tags: [pi, 记忆系统, 操作记录, git]
created: 2026-09-13
updated: 2026-09-13
---

# 2026-09-13 会话操作记录

一天内完成了「记忆系统换代」的完整流程，按时间顺序：

## 1. git 环境（21:50 前后）
- 确认 git 已装（`2.55.0.windows.3`），补配全局身份 `陈建鹏 <129568493+JianpChen@users.noreply.github.com>` 与 Windows 推荐项
- `D:/工作学习/git的测试使用/` 初始化为仓库并**推送到** `https://github.com/JianpChen/test.git`
- 首次 push 失败（直连 GitHub 被 reset）→ 探测到本地代理 `127.0.0.1:7897` → 挂代理成功
- 固化：`git config --global http.https://github.com.proxy http://127.0.0.1:7897`（只对 github.com 生效）

## 2. 卸载旧记忆系统（23:17）
- 删 `pi-memory-agent` 插件（本地路径安装）：移除 settings.json `packages` 条目 + 删源目录（删前逐文件 MD5 与 zip 备份比对一致）
- 删 `memory-profiles` 四处：已装 skill、数据目录、`~/.pi/agent/memory` 软链、D 盘源目录
- 备份：`memory-profiles-backup-20260913-231814.zip`
- 发现：pi 本体**不自带** memory 扩展，删完即无任何记忆系统

## 3. 安装 pi-memory-md（23:20）
- `pi install npm:pi-memory-md` **必失败**（nodejieba 原生模块 → 预编译包在 GitHub Releases，直连不通 → 回退本地编译 → 无 VS）
- 挂代理重试：预编译包下载解包成功，但 node-pre-gyp 在 node 22 报 `Completion callback never invoked!`，code 6 → npm 回滚
- **成功方案**：`--ignore-scripts` 装包（71 个）→ 手动 curl 走代理下 nodejieba 预编译包 → 放置 `build/Release/nodejieba.node` → 手动写 settings.json

## 4. 配置与初始化（23:24）
- `memoryDir = { repoUrl: https://github.com/JianpChen/pi-memory-md.git, localPath: C:/Users/23932/.pi/memory-md, globalMemory: global }`
- 主动开启自动同步 `sessionStart=["pull"]`、`sessionEnd=["push"]`
- 跑 `memory-init.sh` 克隆记忆库（本机**无 jq**，走 grep/sed 回退仍正常）

## 5. 修复 Windows 路径 bug（23:27）
- 报错 `Directory exists but is not a git repo` → 根因是 `gitRoot`（正斜杠）与 `cwd`（反斜杠）**严格比较**永不相等
- 修 `utils.ts` 的 `getProjectMeta()`：对 git 输出路径做 `path.normalize()`，一处修好 3 个调用点（pull / push / memory_check）
- 上游 `main` 分支未修复；补丁归档 `pi-memory-md-Windows路径补丁/`

## 6. 迁移旧记忆（23:31）
- 从 3 个旧记忆体（default/personal/work）提炼，**重新组织**而非照搬
- 产出 4 个新文件 + 2 个分区：`pi的记忆研究`（演进时间线 / 历史上的插件 / 离线检索原理 / 无网移植）、`CPP代码缺陷审查skill`（cpp-defect-review）
- 补充 `global/USER.md`（兴趣）、`global/MEMORY.md`（现状）、新增 `global/软件测试学习.md`
- **有意不迁移**：`### 对话记忆 #N` 逐字稿（过程记录非长期知识）、一次性天气信息
- 旧数据保留在 `~/.pi/agent/memory-agents/`，另打备份 zip

## 7. 污染分析（23:42）
- 实测确认 4 个污染风险点，详见同目录 `记忆隔离与污染分析.md`
- 结论：读取隔离 ✔，写入无沙箱；坑 4（`scope=global` 失效）**当前实际影响**尚未修复

## 待办
- [ ] 坑 4 修复：把 `global/*.md` 移到 `global/core/*.md`（方案 A，推荐），或改 `tools.ts:432`（方案 B）
- [ ] 可选：给上游 VandeeFeng/pi-memory-md 提 issue（坑 3、坑 4 都是真 bug）
